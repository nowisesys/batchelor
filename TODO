* Rename web service directory ws -> api

* Callback::setLogger(): Needs to work with non-memory logger.

* Add user service (logger on user) by injecting object into services from 
  controllers (public service and page controllers).

* Drag and drop for submitting jobs (upload file).

* Support named jobs. The name could be passed in job data, kept in scheduled
  job and made available in job identity.
