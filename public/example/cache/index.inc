<h1>Services</h1>
<p>
    Examples on using cache classes. The cache sub system is divided into backend classes,
    formatters and service. It's possible to stack multiple backends together in a multi
    layer cache (were slower cache has longer lifetime).
</p>
